# Generated by puppet
<%-
$_abs_path = $command =~ /^\//
$_command = if $_abs_path {
 $command
} else {
 "${nrpe::lib_dir}/${command}"
}
-%>
command[<%= $name %>]=<% if $sudo { %>/usr/bin/sudo -u <%= $sudo_user %> <% } %><%= $_command %>
